# Security policy

The current radio protocol is not encrypted or authenticated. Treat RF access as untrusted and keep the Pi command set restricted to exact whitelisted tokens.

Do not report credentials, private images, private keys, or sensitive logs in public issues.

See [Security and safety](docs/operations/security.md).
